import {CustomCountryDetailsPipe} from './custom-country-details.pipe';

describe('CustomCountryDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCountryDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
